production:
  config:
    canvas:
      canvas:
        encryption-secret: "{{ECOSYSTEM_SECRET}}"
        signing-secret: "{{ECOSYSTEM_KEY}}"
      rich-content-service:
        app-host: "https://rce.{{domain}}/"
